# Editor build

Requires Node.js 24 LTS. Run `npm ci` and `npm run build` here, then `python3 tools/package.py` from the repository root. WordPress provides the runtime dependencies listed in the generated `dist/blocks.asset.php`.
